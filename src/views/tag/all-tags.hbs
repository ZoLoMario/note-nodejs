<div class="card-columns">
<!-- <div class="row"> -->
  {{#each tags}}
  <!-- <div class="col-md-3"> -->
    <div class="card my-3">
      <div class="card-body">
        <h4 class="card-title d-flex justify-content-between align-items-center">
          {{ name }} <a href="/tags/edit/{{_id}}"><i class="fas fa-edit"></i></a>
        </h4>
		<div class="ce-example form-group">
			<div class="ce-example__content _ce-example__content--small">
				<div id="{{_id}}" ></div>
			</div>
			<script>
				var data = '{"time":1603361418388,"blocks":[{"type":"list","data":{"style":"ordered","items":' + {{#note}} + '}}],"version":"2.19.0"}';
				var blocks = data.blocks;
				var editor = new EditorJS({
					readOnly: true,
					holder: '{{_id}}',
					tools: tool,
					i18n: il8n,
					data: {blocks},
					onReady: function(){
						editoronReady();
						//saveButton.click();
					},
					onChange: function() {
						console.log('something changed');
					}
				});
				</script>
		</div>
		  <!-- TAG -->
		<div class="form-group">
			<div class="post" id="taglist">
			<ul class="tags">
			{{#note}}
            <li><a href="#">{{note}}</a></li>
			{{/note}}
			</ul> 
			</div>
			</div>
		
		
        <!-- DELETE REQUEST -->
        <form action="/notes/delete/{{_id}}?_method=DELETE" method="POST">
          <input type="hidden" name="_method" value="DELETE">
          <button type="submit" class="btn btn-danger btn-block btn-sm">Delete</button>
        </form>
      </div>
    </div>
  <!-- </div> -->
  {{else}}
  <div class="card mx-auto">
    <div class="card-body">
      <h1>Hello {{user.name}}</h1>
      <p clsss="lead">There are not Notes yet.</p>
      <a href="/notes/add" class="btn btn-success btn-block">Create One!</a>
    </div>
  </div>
  {{/each}}
</div>
